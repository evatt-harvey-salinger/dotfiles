-- Seamless navigation between tmux and vim splits
return {
  'christoomey/vim-tmux-navigator',
  -- No dependencies required
  -- No commands or keys to lazy-load; plugin is always active
  -- config = function()
  --   -- Optional: custom config here
  -- end,
}
